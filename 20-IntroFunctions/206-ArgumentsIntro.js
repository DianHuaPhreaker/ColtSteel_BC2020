// Arguments Intro 

// Note that person is a parameter 
function greet(person) {
    console.log(`Hi, ${person}`);
}

greet("Tom")

function rant(message){
    console.log(message.toUpperCase());
    console.log(message.toUpperCase());
    console.log(message.toUpperCase());
}

rant("hi");